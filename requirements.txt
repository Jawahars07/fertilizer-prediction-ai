# Runtime dependencies for the Fertilizer Prediction Streamlit app.
# Slimmed from a full pip-freeze to only what app.py actually imports, pinned to
# patched releases. This removes the unused Jupyter/dev stack (nbconvert, mistune,
# bleach, GitPython, ipython, Pygments, ...) and vulnerable Pillow, resolving the
# Dependabot security alerts. Model (ferti.pkl, LinearDiscriminantAnalysis) verified
# to load + predict under scikit-learn 1.9.0.
streamlit>=1.54.0
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.0
